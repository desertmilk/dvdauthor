name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: linux-x86_64
            arch: x86_64
            asset: dvdauthor-linux-x86_64.tar.gz
          - os: ubuntu-24.04-arm
            name: linux-arm64
            arch: arm64
            asset: dvdauthor-linux-arm64.tar.gz
          - os: macos-13
            name: macos-x86_64
            arch: x86_64
            asset: dvdauthor-macos-x86_64.tar.gz
          - os: macos-14
            name: macos-arm64
            arch: arm64
            asset: dvdauthor-macos-arm64.tar.gz

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential autoconf automake libtool pkg-config gettext m4 flex bison \
            libdvdread-dev libpng-dev libxml2-dev libfontconfig1-dev \
            libfreetype6-dev libfribidi-dev

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          brew update
          brew install autoconf automake libtool pkg-config gettext m4 flex bison \
            libdvdread libpng libxml2 fontconfig freetype fribidi

      - name: Bootstrap and build
        shell: bash
        run: |
          set -eux
          if [[ "${RUNNER_OS}" == "macOS" ]]; then
            export PATH="$(brew --prefix)/opt/bison/bin:$(brew --prefix)/opt/flex/bin:$PATH"
          fi
          ./bootstrap
          ./configure --prefix="$PWD/_install"
          CPU_COUNT="$(getconf _NPROCESSORS_ONLN 2>/dev/null || sysctl -n hw.ncpu 2>/dev/null || echo 2)"
          make -j"$CPU_COUNT"
          make install-strip

          mkdir -p release
          tar -C "$PWD/_install" -czf "release/${{ matrix.asset }}" .

      - name: Rename artifact for architecture
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -eux
          mv -f "release/${{ matrix.asset }}" "release/dvdauthor-${{ matrix.name }}.tar.gz"

      - name: Create source distribution
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          set -eux
          make dist
          cp -f ./*.tar.gz release/

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}-artifacts
          path: release/*

      - name: Publish GitHub release
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -eux
          cat > release-notes.md <<'EOF'
          ## Downloads

          - Linux x86_64
          - Linux arm64
          - macOS x86_64
          - macOS arm64

          ## Included binaries

          - dvdauthor-linux-x86_64.tar.gz
          - dvdauthor-linux-arm64.tar.gz
          - dvdauthor-macos-x86_64.tar.gz
          - dvdauthor-macos-arm64.tar.gz

          ## Notes

          This release contains native builds for the supported architectures above.
          Choose the archive that matches your platform.
          EOF

          gh release create "${GITHUB_REF_NAME}" --verify-tag --notes-file release-notes.md release/*
